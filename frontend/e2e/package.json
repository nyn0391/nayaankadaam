{
  "name": "frontend-e2e",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "cypress": "^12.0.0",
    "wait-on": "^6.0.1",
    "@types/node": "^18.0.0"
  },
  "scripts": {
    "start": "npm run dev",
    "dev": "vite",
    "e2e:open": "cypress open",
    "e2e:run": "wait-on http://localhost:3000 && cypress run --config baseUrl=http://localhost:3000"
  }
}
