# Frontend README

Frontend skeleton using Vite + React + TypeScript + Material UI.

Run locally:

- cd frontend
- npm install
- npm run dev

The frontend is configured to talk to backend at VITE_API_BASE_URL (set in docker-compose or .env).
